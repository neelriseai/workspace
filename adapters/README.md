# Adapters

Each automation adapter owns the framework-specific integration layer while reusing the shared healing core.

Implemented:
- `adapters/playwright_python`
- `adapters/selenium_python`

Planned extension points:
- `adapters/playwright_java`
- `adapters/selenium_java`
